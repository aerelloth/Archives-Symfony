{% extends 'base.html.twig' %}

{% block title %}Panneau d'administration{% endblock %}

{% block body %}

<div class="container">
	<h1>Panneau d'administration</h1>
	{% if user != 'admin' %}
	
	Ce contenu est protégé.
	<form action="{{ path('login') }}" method="POST">
		<input type="password" placeholder="Mot de passe" name="password">
		<button type="submit">Ok</button>
	</form>
	<a href="{{path('homepage')}}">Retour à l'accueil</a>
	{% elseif user == 'admin' %}

	<div class="liens">	
		<h2>Histoires</h2>
		<a href="{{path('show_stories')}}">Afficher toutes les histoires</a>
		<a href="{{path('add_story')}}">Ajouter une histoire</a>
		<h2>Catégories</h2>
		<a href="{{path('show_categories')}}">Afficher toutes les catégories</a>
		<a href="{{path('add_category')}}">Ajouter une catégorie</a>

		<!-- pour ajout ultérieur -->

		<!--<h2>Chroniques</h2>
		<a href="#">Afficher toutes les chroniques</a>
		<a href="#">Ajouter une chronique</a>
		<h2>Annexes</h2>
		<a href="#">Afficher toutes les annexes</a>
		<a href="#">Ajouter une annexe</a>
		<h2>Pages</h2>
		<a href="#">Afficher toutes les pages</a>
		<a href="#">Ajouter une page</a>-->

		<!-- ------------------- -->

	</div>
	<a href="{{path('logout')}}">Me déconnecter</a>
	{% endif %}
</div>

{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('CSS/style-admin.css') }}">
<style>
	.liens {
		margin: 30px auto 20px auto;
	}
	 .liens a {
	 	display: block;
	 }
</style>
{% endblock %}